import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  input: {
    width: "100%",
    height: 52,
    borderRadius: 10,
    borderWidth: 1,
    borderColor: "#FFF",
    color: "#FFF",
    fontSize: 16,
    padding: 12,
  },
});

export default styles;
